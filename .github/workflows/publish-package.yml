name: Package Publishing

on:
  release:
    types:
      - published
  push:
    branches:
      - 'feature/*'

concurrency:
  group: publishing-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

env:
  NEED_DEV_VERSION_POSTFIX: ${{ startsWith(github.ref_name, 'feature/') || startsWith(github.ref_name, 'release/') }}

jobs:
  release-build:
    name: Build package
    runs-on: ubuntu-24.04
    timeout-minutes: 30

    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          fetch-tags: true
      
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.14"

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          version: '2.4.1'
          virtualenvs-create: true
          virtualenvs-in-project: true
      
      - name: Generate and setup version with development metadata
        id: generate_version_with_development_metadata
        if: env.NEED_DEV_VERSION_POSTFIX == 'true'
        run: |
          SHORT_SHA=$(git rev-parse --short=8 HEAD)
          DEV_NUM=$((16#$SHORT_SHA))
          CURRENT_VERSION=$(poetry version -s)
          VERSION="${CURRENT_VERSION}.dev${DEV_NUM}"
          poetry version "${VERSION}"
          echo "VERSION=${VERSION}" >> $GITHUB_OUTPUT

      - name: Show version with development metadata
        if: env.NEED_DEV_VERSION_POSTFIX == 'true'
        run: |
          echo "Version: ${{ steps.generate_version_with_development_metadata.outputs.VERSION }}"

      - name: Build the Package        
        run: |
          poetry build

      # TODO: Add step wheel build verification (Run example_app test for example)
      # - name: Verify that build is correct
      #   run: smth

      - name: Upload dists
        uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist
          retention-days: 1

  pypi-publish:
    name: Publish package
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    needs:
      - release-build
    permissions:
      id-token: write

    environment:
      name: pypi

    steps:
      - name: Retrieve release distributions
        uses: actions/download-artifact@v7
        with:
          name: dist
          path: dist

      - name: Publish package distributions to PyPI
        uses: pypa/gh-action-pypi-publish@v1.14.0